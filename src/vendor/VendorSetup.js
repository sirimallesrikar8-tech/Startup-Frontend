import VendorLayout from "./VendorLayout";

const VendorSetup = () => {
  return (
    <VendorLayout>
      <div style={{
        background: "#fff",
        borderRadius: "12px",
        padding: "40px",
        minHeight: "400px"
      }}>
        📅 Calendar UI goes here
      </div>
    </VendorLayout>
  );
};

export default VendorSetup;
