import React from "react";
import "./VendorTopbar.css";

const VendorTopbar = () => {
  return (
    <header className="vendor-topbar">
      {/* Generic topbar (can add user menu later) */}
    </header>
  );
};

export default VendorTopbar;
